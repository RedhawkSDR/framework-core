# vim: noet: softtabstop=0

xmldir = $(prefix)/dev/devices/bad_init_device/
dist_xml_DATA = ../bad_init_device.scd.xml ../bad_init_device.prf.xml ../bad_init_device.spd.xml
devdir = $(prefix)/dev/devices/bad_init_device/python/

distclean-local:
	rm -f config.*
	rm -rf autom4te.cache
	rm -f acinclude.m4
	rm -f aclocal.m4
	rm -f configure
	rm -f depcomp
	rm -f install-sh
	rm -f ltmain.sh
	rm -f Makefile.in
	rm -f missing
	rm -rf .deps

# Sources are auto-included from a file generated by the REDHAWK IDE. You may
# remove the following lines if you wish to manually control these options.
include $(srcdir)/Makefile.am.ide
nobase_dist_dev_SCRIPTS = $(redhawk_SCRIPTS_auto)
nobase_dist_dev_DATA = $(redhawk_DATA_auto)
